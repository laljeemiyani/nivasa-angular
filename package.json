{
  "name": "nivasa-web",
  "version": "1.0.0",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "client": "npm run start --workspace=frontend",
    "server": "npm run dev-full --workspace=backend",
    "build": "npm run build --workspaces --if-present",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "kill-ports": "lsof -ti :5001 | xargs kill -9 2>/dev/null; lsof -ti :4200 | xargs kill -9 2>/dev/null; echo 'Ports 5001 and 4200 cleared (if they were in use).'"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
